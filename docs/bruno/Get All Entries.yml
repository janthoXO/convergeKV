info:
  name: Get All Entries
  type: grpc
  seq: 5

grpc:
  url: "{{seed}}"
  method: /debug.DebugService/ScanAll
  methodType: server-streaming
  protoFilePath: ../../proto/debug/debug.proto
  auth: inherit
