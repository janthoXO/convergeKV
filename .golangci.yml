version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam
    - copyloopvar
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - unparam
